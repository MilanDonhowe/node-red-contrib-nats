node-red-contrib-nats
=====================

Dead simple node-red wrapper for nats.js.

Install
-------

Run the following command in the root directory of your Node-RED install

```
npm install @mylawn/node-red-contrib-nats
```
